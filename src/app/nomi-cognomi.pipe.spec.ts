import { NomiCognomiPipe } from './nomi-cognomi.pipe';

describe('NomiCognomiPipe', () => {
  it('create an instance', () => {
    const pipe = new NomiCognomiPipe();
    expect(pipe).toBeTruthy();
  });
});
